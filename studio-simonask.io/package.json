{
  "name": "simonaskio",
  "private": true,
  "version": "1.0.0",
  "main": "package.json",
  "license": "UNLICENSED",
  "scripts": {
    "build": "sanity build",
    "deploy": "sanity deploy",
    "deploy-graphql": "sanity graphql deploy",
    "dev": "sanity dev",
    "start": "sanity start",
    "migration:technologies": "sanity migration run rename-project-tags-field --no-dry-run --no-confirm && sanity migration run clear-project-technology-refs --no-dry-run --no-confirm && sanity migration run delete-project-tag-documents --no-dry-run --no-confirm && sanity migration run create-project-technology-documents --no-dry-run --no-confirm && sanity migration run restore-project-technology-refs --no-dry-run --no-confirm"
  },
  "keywords": [
    "sanity"
  ],
  "dependencies": {
    "@sanity/code-input": "^7.1.1",
    "@sanity/vision": "^5.26.0",
    "@selvklart/sanity-block-selector": "^4.0.4",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "sanity": "^5.25.1",
    "styled-components": "^6.1.18"
  },
  "devDependencies": {
    "@sanity/eslint-config-studio": "^6.0.0",
    "@types/react": "^19.2.14",
    "eslint": "^9.28",
    "prettier": "^3.5",
    "typescript": "^5.8"
  },
  "prettier": {
    "bracketSpacing": false,
    "printWidth": 100,
    "semi": false,
    "singleQuote": true
  }
}
