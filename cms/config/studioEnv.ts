/**
 * Default Sanity project config for hosted Studio deploys.
 * Project ID and dataset are public (same as NEXT_PUBLIC_* on the site).
 * Override with .env.local for local dev if needed.
 */
export const defaultStudioProjectId = 'au2uzesy'
export const defaultStudioDataset = 'production'
export const defaultPreviewOrigin = 'https://simonask.io'
